import React from "react";
import { Typography } from "@mui/material";

const SalesGraph = () => {
  return (
    <>
      <Typography>Hello world I am a graph</Typography>
    </>
  );
};

export default React.memo(SalesGraph);
